import React from "react";

const UserContext = React.createContext()
// it is a hook that allows you to pass data through the component tree without having to pass props down manually at every level.

export deafult UserContext;